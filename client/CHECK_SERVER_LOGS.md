# 🔍 检查服务器日志

## 快速诊断

在生产服务器上运行：

```bash
# 查看最近的错误日志
pm2 logs 0 --lines 100 --err

# 或查看所有日志
pm2 logs 0 --lines 100

# 过滤关键错误
pm2 logs 0 --lines 200 | grep -i "error\|fail\|500"
```

## 可能的问题

### 1. Database 模型不存在

`/api/search/databases` 接口需要 Database 模型，但可能没有创建。

**解决方案**：需要创建 Database 模型或修改接口不依赖它。

### 2. Advertisement 模型问题

`/api/advertisements/public` 接口报 500 错误。

## 临时解决方案

### 方案 1: 重新登录

401 错误说明需要重新登录：
1. 退出登录
2. 清除浏览器缓存
3. 重新登录

### 方案 2: 检查服务器日志

```bash
cd /var/www/html/knowledge-base-system/client/server
pm2 logs 0 --lines 200
```

查找具体的错误信息，然后告诉我错误内容。

## 下一步

请运行以下命令并把输出发给我：

```bash
pm2 logs 0 --lines 100 | grep -A 5 -B 5 "500\|Error\|fail"
```

这样我可以看到具体是什么错误导致 500。
