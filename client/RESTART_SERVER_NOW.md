# ⚠️ 需要立即重启服务器

## 问题已修复

提现配置API路由已修复，但**必须重启服务器**才能生效。

## 🚀 重启步骤

### 1. 停止当前服务器
在运行服务器的终端窗口按：
```
Ctrl + C
```

### 2. 重新启动服务器
```bash
npm start
```

### 3. 验证API是否正常
打开新终端，运行测试：
```bash
node server/scripts/testPublicAPI.js
```

应该看到：
```
✅ API工作正常
余额提现最低金额: $1
USDT提现最低金额: $1
```

## 📱 通知用户

重启服务器后，通知用户：
1. **刷新浏览器页面** (Ctrl+F5 或 Cmd+Shift+R)
2. **重新进入提现页面**
3. **查看最低金额是否更新为 $1**

## ✅ 完成后

- 用户提现 $1 应该可以成功
- 管理员在后台修改配置后，用户刷新页面即可看到新配置
