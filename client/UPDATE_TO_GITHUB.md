# 🔄 更新代码到 GitHub

## ⚡ 快速更新（3 步）

```bash
# 1. 添加修改的文件
git add .

# 2. 提交更改
git commit -m "fix: 修复备份功能，现在会备份前端源代码"

# 3. 推送到 GitHub
git push origin main
```

就这么简单！✅

---

## 📝 详细步骤

### 步骤 1: 查看修改的文件

```bash
# 查看哪些文件被修改了
git status

# 查看具体的修改内容
git diff
```

### 步骤 2: 添加文件到暂存区

```bash
# 添加所有修改的文件
git add .

# 或者只添加特定文件
git add server/services/backupService.js
```

### 步骤 3: 提交更改

```bash
# 提交并添加说明
git commit -m "fix: 修复备份功能，现在会备份前端源代码

- 修改 backupClientCode 方法
- 现在会备份 src/ 目录（前端源代码）
- 备份前端配置文件（package.json, vite.config.ts 等）
- 备份前端构建文件（dist/ 目录）
"
```

### 步骤 4: 推送到 GitHub

```bash
# 推送到远程仓库
git push origin main

# 如果是第一次推送，使用
git push -u origin main
```

---

## 🏷️ 创建新版本标签（可选）

如果这是一个重要的更新，可以创建版本标签：

```bash
# 创建版本标签
git tag -a v1.1.1 -m "修复备份功能，支持完整的前后端代码备份"

# 推送标签
git push origin v1.1.1
```

---

## 📋 提交消息规范

使用清晰的提交消息：

**格式**：`<type>: <subject>`

**类型**：
- `feat`: 新功能
- `fix`: Bug 修复
- `docs`: 文档更新
- `style`: 代码格式
- `refactor`: 重构
- `perf`: 性能优化
- `test`: 测试
- `chore`: 构建/工具

**示例**：
```bash
git commit -m "fix: 修复备份功能不备份前端源代码的问题"
git commit -m "feat: 添加前端源代码备份功能"
git commit -m "docs: 更新备份功能文档"
```

---

## 🔍 常用 Git 命令

### 查看状态

```bash
# 查看当前状态
git status

# 查看提交历史
git log --oneline

# 查看最近 5 次提交
git log -5
```

### 撤销操作

```bash
# 撤销工作区的修改
git checkout -- <file>

# 撤销暂存区的文件
git reset HEAD <file>

# 修改最后一次提交
git commit --amend
```

### 分支操作

```bash
# 查看分支
git branch

# 创建新分支
git checkout -b feature/new-feature

# 切换分支
git checkout main

# 合并分支
git merge feature/new-feature
```

---

## 🚀 完整的更新流程

```bash
# 1. 确保在正确的分支
git branch
# 应该显示 * main

# 2. 拉取最新代码（如果有其他人也在更新）
git pull origin main

# 3. 查看修改
git status
git diff

# 4. 添加文件
git add .

# 5. 提交
git commit -m "fix: 修复备份功能，支持前后端完整代码备份"

# 6. 推送
git push origin main

# 7. 验证
# 访问 GitHub 仓库查看更新
```

---

## ⚠️ 注意事项

### 确保不提交敏感文件

```bash
# 检查 .gitignore 是否正确
cat .gitignore

# 确保以下文件被忽略：
# - .env
# - node_modules/
# - *.log
# - backups/
# - temp/
```

### 如果不小心提交了敏感文件

```bash
# 从 Git 中删除但保留本地文件
git rm --cached server/.env

# 提交删除操作
git commit -m "chore: 移除敏感文件"

# 推送
git push origin main
```

---

## 🔄 同步到生产服务器

更新 GitHub 后，在生产服务器上拉取更新：

```bash
# SSH 连接到服务器
ssh root@your-server-ip

# 进入项目目录
cd /var/www/html/knowledge-base-system

# 拉取最新代码
git pull origin main

# 如果有依赖更新
npm install
cd server && npm install

# 重启应用
pm2 restart knowledge-base

# 查看日志
pm2 logs knowledge-base
```

---

## 💡 提示

1. **经常提交**：小步提交，便于回滚
2. **清晰的消息**：让别人（和未来的你）知道改了什么
3. **先拉后推**：推送前先拉取最新代码
4. **测试后提交**：确保代码能正常运行
5. **使用分支**：重要功能在分支上开发

---

## 📚 更多资源

- **Git 文档**: https://git-scm.com/doc
- **GitHub 指南**: https://docs.github.com/
- **提交规范**: https://www.conventionalcommits.org/

---

**准备好了吗？** 运行上面的快速更新命令吧！🚀
