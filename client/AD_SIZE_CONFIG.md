# 广告框尺寸配置说明

## 📐 当前尺寸设置

### 默认配置
```css
.ad-content {
  max-height: 600px;  /* 最大高度 */
}

.ad-content img {
  max-height: 600px;  /* 图片最大高度 */
  object-fit: contain; /* 保持图片比例 */
}
```

### 宽度
- **最大宽度**: 由父容器决定
  - 搜索页面：跟随页面内容宽度（通常是容器的100%）
  - 管理后台：跟随卡片宽度

### 高度
- **最大高度**: 600px
- **实际高度**: 根据内容自动调整
- **超出处理**: 隐藏超出部分（`overflow: hidden`）

## 🎯 不同位置的广告尺寸

### 1. 搜索页面广告
**位置**: 搜索结果上方

**容器代码**:
```tsx
<div className="bg-white rounded-lg shadow-sm border-l-4 border-blue-500 overflow-hidden">
  <div className="ad-content" dangerouslySetInnerHTML={{ __html: ad.content }} />
</div>
```

**尺寸特点**:
- 宽度：自适应容器（通常是页面内容宽度）
- 最大高度：600px
- 响应式：在移动设备上自动缩小

### 2. 管理后台预览
**位置**: 内容管理 > 广告列表

**容器代码**:
```tsx
<div className="text-gray-600 text-sm mt-2 bg-gray-50 rounded overflow-hidden ad-content">
  {/* 广告内容 */}
</div>
```

**尺寸特点**:
- 宽度：跟随卡片宽度
- 最大高度：600px
- 用于预览效果

## 🔧 如何自定义尺寸

### 方法1：修改全局CSS（推荐）
**文件**: `src/styles/globals.css`

```css
/* 修改最大高度 */
.ad-content {
  max-height: 800px;  /* 改为800px */
}

.ad-content img {
  max-height: 800px;  /* 改为800px */
}
```

### 方法2：为不同位置设置不同尺寸

#### 搜索页面专用样式
```css
/* 搜索页面的广告 */
.search-ad .ad-content {
  max-height: 400px;  /* 搜索页面较小 */
}

.search-ad .ad-content img {
  max-height: 400px;
}
```

然后在Search.tsx中使用：
```tsx
<div className="search-ad bg-white rounded-lg ...">
  <div className="ad-content" ... />
</div>
```

#### 首页横幅广告
```css
/* 首页横幅广告 */
.home-banner-ad .ad-content {
  max-height: 300px;  /* 横幅较矮 */
}

.home-banner-ad .ad-content img {
  max-height: 300px;
  object-fit: cover;  /* 裁剪填充 */
}
```

### 方法3：设置最大宽度

如果需要限制宽度：
```css
.ad-content {
  max-width: 1200px;  /* 最大宽度 */
  max-height: 600px;
  margin: 0 auto;     /* 居中显示 */
}
```

## 📊 推荐的广告尺寸

### 横幅广告（Banner）
```
尺寸: 1200x300px 或 1920x400px
比例: 4:1 或 16:3
用途: 页面顶部、搜索结果上方
CSS: max-height: 400px
```

### 侧边栏广告（Sidebar）
```
尺寸: 300x250px 或 300x600px
比例: 6:5 或 1:2
用途: 页面侧边栏
CSS: max-height: 600px
```

### 方形广告（Square）
```
尺寸: 600x600px
比例: 1:1
用途: 内容中间插入
CSS: max-height: 600px
```

### 全屏广告（Fullscreen）
```
尺寸: 1920x1080px
比例: 16:9
用途: 弹窗广告
CSS: max-height: 800px
```

## 🎨 图片适配模式

### object-fit 属性说明

#### contain（当前使用）
```css
object-fit: contain;
```
- ✅ 保持图片完整
- ✅ 保持图片比例
- ⚠️ 可能有空白区域

**效果**:
```
┌─────────────────┐
│                 │  ← 上下可能有空白
│   ┌─────────┐   │
│   │  图片   │   │
│   └─────────┘   │
│                 │  ← 上下可能有空白
└─────────────────┘
```

#### cover（填充模式）
```css
object-fit: cover;
```
- ✅ 填满整个区域
- ✅ 无空白
- ⚠️ 图片可能被裁剪

**效果**:
```
┌─────────────────┐
│   图片填满整个   │  ← 无空白
│   容器区域       │
└─────────────────┘
```

#### fill（拉伸模式）
```css
object-fit: fill;
```
- ✅ 填满整个区域
- ❌ 可能变形
- ❌ 不保持比例

### 如何选择

| 广告类型 | 推荐模式 | 原因 |
|---------|---------|------|
| 横幅广告 | cover | 填满宽度，无空白 |
| 方形广告 | contain | 保持完整 |
| 产品图片 | contain | 不裁剪产品 |
| 背景图片 | cover | 填满背景 |

## 💡 实际应用示例

### 示例1：标准横幅广告
```css
/* 在 globals.css 中 */
.banner-ad .ad-content {
  max-height: 300px;
}

.banner-ad .ad-content img {
  max-height: 300px;
  object-fit: cover;  /* 填充模式 */
}
```

```tsx
// 在 Search.tsx 中
<div className="banner-ad bg-white rounded-lg ...">
  <div className="ad-content" dangerouslySetInnerHTML={{ __html: ad.content }} />
</div>
```

### 示例2：侧边栏广告
```css
.sidebar-ad .ad-content {
  max-width: 300px;
  max-height: 600px;
}

.sidebar-ad .ad-content img {
  max-height: 600px;
  object-fit: contain;
}
```

### 示例3：响应式广告
```css
.responsive-ad .ad-content {
  max-height: 600px;
}

.responsive-ad .ad-content img {
  max-height: 600px;
  object-fit: contain;
}

/* 移动设备 */
@media (max-width: 768px) {
  .responsive-ad .ad-content {
    max-height: 300px;
  }
  
  .responsive-ad .ad-content img {
    max-height: 300px;
  }
}
```

## 🔍 当前配置总结

### 宽度
- ❌ **未限制** - 跟随父容器宽度
- 搜索页面：通常是页面内容宽度（约1200px）
- 管理后台：跟随卡片宽度

### 高度
- ✅ **已限制** - 最大600px
- 超出部分：隐藏
- 图片模式：contain（保持比例）

### 如何修改
1. 打开 `src/styles/globals.css`
2. 找到 `.ad-content` 样式
3. 修改 `max-height` 值
4. 保存文件，刷新页面

## 📝 配置建议

### 如果广告图片很大
```css
.ad-content {
  max-height: 400px;  /* 降低高度 */
}

.ad-content img {
  max-height: 400px;
  object-fit: cover;  /* 使用裁剪模式 */
}
```

### 如果需要显示完整图片
```css
.ad-content {
  max-height: 800px;  /* 增加高度 */
}

.ad-content img {
  max-height: 800px;
  object-fit: contain;  /* 保持完整 */
}
```

### 如果需要限制宽度
```css
.ad-content {
  max-width: 1200px;  /* 最大宽度 */
  max-height: 600px;
  margin: 0 auto;     /* 居中 */
}
```

## ✅ 总结

**当前设置**:
- 宽度：自适应（无限制）
- 高度：最大600px
- 图片：保持比例（contain）

**推荐设置**:
- 横幅广告：1200x300px，max-height: 300px
- 侧边栏：300x600px，max-height: 600px
- 方形广告：600x600px，max-height: 600px

**修改位置**: `src/styles/globals.css` 中的 `.ad-content` 样式
